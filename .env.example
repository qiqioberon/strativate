NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=YOUR_PUBLISHABLE_KEY
# Server only. Never give this key a NEXT_PUBLIC_ prefix.
SUPABASE_SECRET_KEY=YOUR_SUPABASE_SECRET_KEY
APP_URL=http://localhost:3000
# Public storefront and Mentee commerce rollout. Safe default is off.
FEATURE_DIGITAL_PRODUCTS=false
# Midtrans environment: sandbox or production.
MIDTRANS_ENV=sandbox
NEXT_PUBLIC_MIDTRANS_CLIENT_KEY=YOUR_MIDTRANS_CLIENT_KEY
MIDTRANS_SERVER_KEY=YOUR_MIDTRANS_SERVER_KEY
# Optional direct/session-pooler Postgres connection for migrations/imports:
# SUPABASE_DB_URL=postgresql://USER:PASSWORD@HOST:5432/postgres

# Google Calendar OAuth (server only; never prefix these with NEXT_PUBLIC_).
GOOGLE_OAUTH_CLIENT_ID=YOUR_GOOGLE_OAUTH_CLIENT_ID
GOOGLE_OAUTH_CLIENT_SECRET=YOUR_GOOGLE_OAUTH_CLIENT_SECRET
# Must exactly match an Authorized redirect URI in Google Cloud Console.
# Production Vercel example: https://strativate.vercel.app/api/google-calendar/callback
GOOGLE_OAUTH_REDIRECT_URI=http://localhost:3000/api/google-calendar/callback
# 32 random bytes, base64 encoded. Used only to encrypt refresh tokens at rest.
# Example generation: openssl rand -base64 32
GOOGLE_TOKEN_ENCRYPTION_KEY=YOUR_32_BYTE_BASE64_KEY

# Zoom Server-to-Server OAuth. New mentoring sessions use Zoom for video meetings.
ZOOM_ACCOUNT_ID=YOUR_ZOOM_ACCOUNT_ID
ZOOM_CLIENT_ID=YOUR_ZOOM_CLIENT_ID
ZOOM_CLIENT_SECRET=YOUR_ZOOM_CLIENT_SECRET
ZOOM_DEFAULT_HOST_USER_ID=YOUR_ZOOM_LICENSED_HOST_USER_ID
ZOOM_WEBHOOK_SECRET_TOKEN=YOUR_ZOOM_WEBHOOK_SECRET_TOKEN

# Transactional commerce email (server only).
# Supabase Auth email bodies remain configured through Supabase Email Templates.
RESEND_API_KEY=YOUR_RESEND_API_KEY
STRATIVATE_EMAIL_FROM="Strativate <no-reply@YOUR_VERIFIED_DOMAIN>"
# Secret bearer token for /api/internal/email-outbox retry worker.
CRON_SECRET=YOUR_RANDOM_CRON_SECRET
