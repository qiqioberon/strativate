'use client'

import { useState } from 'react'
import { ArrowRight, Bell, CalendarDays, ChevronRight, MessageCircle, Clock3, FileText, LayoutDashboard, LibraryBig, Menu, ReceiptText, ShoppingCart, Sparkles, UsersRound, X } from 'lucide-react'
import { useAccount } from '@/components/auth/account-provider'
import { ProfileForm } from '@/components/auth/profile-form'
import { BrandLogo } from '@/components/brand/brand-logo'
import { RoleCalendar } from '@/components/calendar/role-calendar'
import { CartView } from '@/components/commerce/cart-view'
import { UserOrderHistory } from '@/components/commerce/user-order-history'
import { DashboardSidebarUtilities } from '@/components/dashboard/dashboard-sidebar-utilities'
import { DashboardNotificationCenter } from '@/components/dashboard/notification-center'
import { DashboardTopbarActions } from '@/components/dashboard/dashboard-topbar-actions'
import { MentorAvailabilityExplorer } from '@/components/dashboard/mentor-availability-explorer'
import { IntensiveMentoringEntitlements } from '@/components/dashboard/intensive-mentoring-entitlements'
import { PrivateMentoringSessions } from '@/components/dashboard/private-mentoring-sessions'
import { displayName } from '@/lib/auth/rules'
import { publicContact } from '@/lib/content/brand'
import type { Notification } from '@/lib/supabase/database.types'
import { formatRupiah } from '@/lib/commerce/money'
import type { ActiveCart, OrderWithItems, OwnedDigitalProductView } from '@/lib/commerce/types'
import type { IntensiveMentoringEntitlementView, PrivateMentoringSessionFocusView, PrivateMentoringSessionView } from '@/lib/private-mentoring/types'

type Section = 'overview' | 'mentoring' | 'mentorAvailability' | 'schedule' | 'library' | 'cart' | 'orders' | 'notifications' | 'profile'
function buildNav(digitalProductsEnabled:boolean):{id:Section;label:string;icon:typeof LayoutDashboard}[]{return[{id:'overview',label:'Ringkasan',icon:LayoutDashboard},{id:'mentoring',label:'Mentoring saya',icon:UsersRound},{id:'mentorAvailability',label:'Ketersediaan mentor',icon:Clock3},{id:'schedule',label:'Jadwal',icon:CalendarDays},...(digitalProductsEnabled?[{id:'library' as const,label:'Produk Digital Saya',icon:LibraryBig}]:[]),{ id: 'cart', label: 'Keranjang', icon: ShoppingCart },{id:'orders',label:'Pesanan & pembayaran',icon:ReceiptText}]}
export function DashboardClient({digitalProductsEnabled,ownedDigitalProducts,cart,commerceOrders,privateMentoringSessions=[],intensiveMentoringEntitlements=[],sessionFocuses=[]}:{digitalProductsEnabled:boolean;ownedDigitalProducts:OwnedDigitalProductView[];cart:ActiveCart;commerceOrders:OrderWithItems[];privateMentoringSessions?:PrivateMentoringSessionView[];intensiveMentoringEntitlements?:IntensiveMentoringEntitlementView[];sessionFocuses?:PrivateMentoringSessionFocusView[]}){
 const account=useAccount();const accountName=displayName(account);const nav=buildNav(digitalProductsEnabled);const[section,setSection]=useState<Section>('overview');const[previousSection,setPreviousSection]=useState<Section>('overview');const[mobile,setMobile]=useState(false)
 const open=(next:Section)=>{if(next!==section)setPreviousSection(section);setSection(next);setMobile(false)}
 const[relatedTarget,setRelatedTarget]=useState<{entity:string|null;id:string|null}|null>(null);const openNotification=(item:Notification)=>{setRelatedTarget({entity:item.related_entity,id:item.related_entity_id});if(item.related_entity==='order')open('orders');else if(item.related_entity==='session'||item.related_entity==='enrollment')open('mentoring');else open('overview')};const hasPendingPayment=commerceOrders.some(order=>order.status==='pending_payment')
 return <div className="workspace"><aside className={`workspace-sidebar ${mobile?'open':''}`}><div className="workspace-brand"><BrandLogo/><button type="button" className="workspace-close" onClick={()=>setMobile(false)} aria-label="Tutup navigasi"><X/></button></div><div className="workspace-profile"><span className="workspace-avatar">{accountName.slice(0,1)}</span><div><strong>{accountName}</strong><span>Akun peserta</span></div><ChevronRight/></div><nav className="workspace-nav" aria-label="Navigasi ruang belajar">{nav.map(({id,label,icon:Icon})=><button type="button" key={id} className={section===id?'active':''} onClick={()=>open(id)}><Icon/>{label}{id==='cart'&&cart.items.length>0?<b>{cart.items.length}</b>:null}{id==='orders'&&hasPendingPayment?<i/>:null}</button>)}<span className="workspace-divider"/><button type="button" className={section==='notifications'?'active':''} onClick={()=>open('notifications')}><Bell/>Notifikasi</button><button type="button" className={section==='profile'?'active':''} onClick={()=>open('profile')}><UsersRound/>Profil</button></nav><div className="workspace-side-footer"><DashboardSidebarUtilities/></div></aside>{mobile&&<button type="button" className="workspace-scrim" onClick={()=>setMobile(false)} aria-label="Tutup menu"/>}<main className="workspace-main"><header className="workspace-topbar"><button type="button" className="workspace-menu" onClick={()=>setMobile(true)} aria-label="Buka navigasi"><Menu/></button><span className="workspace-context">{nav.find(item=>item.id===section)?.label||'Ruang belajar'}</span><div className="workspace-actions"><DashboardTopbarActions role="mentee" onEditProfile={()=>open('profile')} onOpenNotification={openNotification}/></div></header><div className="workspace-content">{section==='overview'&&<Overview name={accountName} open={open} sessions={privateMentoringSessions} products={ownedDigitalProducts} orders={commerceOrders} cart={cart} digitalProductsEnabled={digitalProductsEnabled}/>} {section==='mentoring'&&<><Title eyebrow="Mentoring saya" title="Kelola setiap sesi mentoring." detail="Pantau Private Mentoring per sesi dan entitlement Intensive Mentoring yang sudah dibeli dari satu tempat."/><PrivateMentoringSessions sessions={privateMentoringSessions} sessionFocuses={sessionFocuses} focusSessionId={relatedTarget?.entity==='session'?relatedTarget.id:null} focusEnrollmentId={relatedTarget?.entity==='enrollment'?relatedTarget.id:null}/><IntensiveMentoringEntitlements entitlements={intensiveMentoringEntitlements}/></>}{section==='mentorAvailability'&&<><Title eyebrow="Ketersediaan mentor" title="Lihat mentor yang benar-benar punya slot tersedia." detail="Cari mentor berdasarkan nama, level, tanggal, dan waktu. Informasi ini untuk discovery; admin tetap menangani penugasan dan penjadwalan."/><MentorAvailabilityExplorer/></>}{section==='schedule'&&<RoleCalendar role="mentee"/>}{section==='library'&&<DigitalProductLibrary products={ownedDigitalProducts}/>} {section==='cart'&&<CartView cart={cart} embedded onBack={()=>open(previousSection==='cart'?'overview':previousSection)}/>} {section==='orders'&&<Orders commerceOrders={commerceOrders} focusOrderId={relatedTarget?.entity==='order'?relatedTarget.id:null}/>} {section==='notifications'&&<><Title eyebrow="Notifikasi" title="Pembaruan untuk akunmu." detail="Pembayaran, mentoring, jadwal, dan meeting update berasal dari backend realtime."/><DashboardNotificationCenter onOpenRelated={openNotification}/></>} {section==='profile'&&<ProfileForm/>}</div></main><a className="dashboard-whatsapp-fab" href={publicContact.whatsapp} target="_blank" rel="noopener noreferrer" aria-label="Hubungi Strativate melalui WhatsApp" title="Hubungi Strativate"><MessageCircle aria-hidden="true"/><span>WhatsApp</span></a></div>
}
function Title({eyebrow,title,detail}:{eyebrow:string;title:string;detail:string}){return <div className="workspace-page-title"><div><p className="kicker">{eyebrow}</p><h1>{title}</h1><p>{detail}</p></div></div>}
function sessionLabel(status:string){if(status==='pending_payment')return'Menunggu pembayaran';if(status==='paid')return'Lunas';if(status==='payment_failed')return'Pembayaran gagal';if(status==='expired')return'Kedaluwarsa';if(status==='cancelled')return'Dibatalkan';if(status==='awaiting_focus')return'Menunggu fokus';if(status==='awaiting_scheduling')return'Menunggu admin';if(status==='scheduled')return'Terjadwal';if(status==='completed')return'Selesai';return status.replaceAll('_',' ')}
function Overview({name,open,sessions,products,orders,cart,digitalProductsEnabled}:{name:string;open:(section:Section)=>void;sessions:PrivateMentoringSessionView[];products:OwnedDigitalProductView[];orders:OrderWithItems[];cart:ActiveCart;digitalProductsEnabled:boolean}){const awaitingFocus=sessions.filter(session=>session.status==='awaiting_focus').length;const pendingPayments=orders.filter(order=>order.status==='pending_payment').length;const nextSession=sessions.filter(session=>session.status==='scheduled'&&session.scheduledStartAt).sort((a,b)=>new Date(a.scheduledStartAt!).getTime()-new Date(b.scheduledStartAt!).getTime())[0];const latestOrder=orders[0];return <><Title eyebrow="Ruang belajar" title={`Selamat datang, ${name}.`} detail="Ringkasan ini mengambil data dari mentoring, produk digital, keranjang, dan pesanan yang sudah ada."/>{(awaitingFocus>0||pendingPayments>0)?<section className="workspace-card overview-attention"><p className="kicker">Perlu tindakan</p>{awaitingFocus>0?<button type="button" onClick={()=>open('mentoring')}><Clock3 aria-hidden="true"/><span><strong>{awaitingFocus} sesi membutuhkan fokus</strong><small>Pilih topik agar admin dapat melanjutkan penjadwalan.</small></span><ArrowRight aria-hidden="true"/></button>:null}{pendingPayments>0?<button type="button" onClick={()=>open('orders')}><ReceiptText aria-hidden="true"/><span><strong>{pendingPayments} pembayaran masih pending</strong><small>Buka pesanan untuk melanjutkan pembayaran yang valid.</small></span><ArrowRight aria-hidden="true"/></button>:null}</section>:null}<div className="overview-metric-grid"><button type="button" className="workspace-card overview-metric" onClick={()=>open('mentoring')}><UsersRound aria-hidden="true"/><span>Sesi mentoring</span><strong>{sessions.length}</strong><small>{sessions.filter(session=>session.status==='completed').length} selesai</small></button>{digitalProductsEnabled?<button type="button" className="workspace-card overview-metric" onClick={()=>open('library')}><LibraryBig aria-hidden="true"/><span>Produk digital</span><strong>{products.length}</strong><small>Dimiliki</small></button>:null}<button type="button" className="workspace-card overview-metric" onClick={()=>open('orders')}><ReceiptText aria-hidden="true"/><span>Pesanan</span><strong>{orders.length}</strong><small>{pendingPayments} pending</small></button><button type="button" className="workspace-card overview-metric" onClick={()=>open('cart')}><ShoppingCart aria-hidden="true"/><span>Keranjang</span><strong>{cart.items.length}</strong><small>{cart.items.length?formatRupiah(cart.totalAmount):'Kosong'}</small></button></div><div className="workspace-grid-two overview-detail-grid"><section className="workspace-card"><p className="kicker">Sesi berikutnya</p>{nextSession?<><h3>Sesi {nextSession.sessionNumber} · {nextSession.focusName??'Fokus mentoring'}</h3><p className="muted">{new Intl.DateTimeFormat('id-ID',{dateStyle:'medium',timeStyle:'short'}).format(new Date(nextSession.scheduledStartAt!))} · {nextSession.mentorName??'Mentor belum ditetapkan'}</p><button className="text-link" type="button" onClick={()=>open('schedule')}>Lihat jadwal <ArrowRight/></button></>:<><h3>Belum ada sesi terjadwal.</h3><p className="muted">Jadwal akan muncul setelah fokus dan penugasan admin selesai.</p><button className="text-link" type="button" onClick={()=>open('mentoring')}>Lihat mentoring <ArrowRight/></button></>}</section><section className="workspace-card"><p className="kicker">Pesanan terbaru</p>{latestOrder?<><h3>{latestOrder.items[0]?.name_snapshot??'Pesanan Strativate'}{latestOrder.items.length>1?` +${latestOrder.items.length-1} item lainnya`:''}</h3><p className="muted">{formatRupiah(latestOrder.total_amount)} · {sessionLabel(latestOrder.status)}</p><button className="text-link" type="button" onClick={()=>open('orders')}>Lihat pesanan <ArrowRight/></button></>:<><h3>Belum ada pesanan.</h3><p className="muted">Pesanan baru akan muncul di sini.</p><a className="button button-outline" href="/program">Lihat Program <ArrowRight/></a></>}</section></div></>}
function DigitalProductLibrary({products}:{products:OwnedDigitalProductView[]}){return <><Title eyebrow="Produk Digital Saya" title="Produk digital yang sudah Anda beli." detail="Hanya item dari pesanan yang sudah terverifikasi lunas yang tampil di sini."/>{products.length>0?<div className="resource-grid">{products.map(product=><section className="workspace-card resource-card" key={product.order_item_id}>{product.imageUrl?<>{/* eslint-disable-next-line @next/next/no-img-element */}<img src={product.imageUrl} alt={`Sampul ${product.name_snapshot}`} className="resource-card-cover"/></>:<FileText aria-hidden="true"/>}<p className="kicker">Dibeli {new Intl.DateTimeFormat('id-ID',{dateStyle:'medium'}).format(new Date(product.purchased_at))}</p><h3>{product.name_snapshot}</h3><div className="resource-card__meta"><p>{formatRupiah(product.unit_price_amount)}</p><span className="resource-card__type">{product.contentType?product.contentType.toUpperCase():'Materi'}</span></div>{product.contentReady?<a className="button button-primary resource-card__action" href={`/dashboard/produk-digital/${product.product_id}`}>{product.contentType==='video'?'Tonton video':'Buka materi'} <ArrowRight/></a>:<p className="muted">Materi sedang disiapkan.</p>}</section>)}</div>:<WorkspaceEmptyState title="Anda belum memiliki Produk Digital." detail="Produk digital yang sudah Anda beli akan muncul di sini setelah pembayarannya terverifikasi." href="/produk-digital" action="Lihat Produk Digital"/>}<a className="button button-outline" href="/produk-digital">Lihat Produk Digital di Website</a></>}
function Orders({commerceOrders,focusOrderId}:{commerceOrders:OrderWithItems[];focusOrderId?:string|null}){return <><Title eyebrow="Pesanan & pembayaran" title="Riwayat pesanan Anda." detail="Cari, filter, dan buka detail pesanan tanpa menelusuri daftar kartu panjang."/><UserOrderHistory orders={commerceOrders} focusOrderId={focusOrderId}/></>}
function WorkspaceEmptyState({title,detail,href,action}:{title:string;detail:string;href:string;action:string}){return <section className="workspace-card booking-panel"><Sparkles aria-hidden="true"/><h3>{title}</h3><p className="panel-copy">{detail}</p><a className="button button-outline" href={href}>{action} <ArrowRight/></a></section>}
function SimplePage({title,detail}:{title:string;detail:string}){return <><Title eyebrow="Ruang belajar" title={title} detail={detail}/><section className="empty-order"><Sparkles/><p>Gunakan navigasi untuk melanjutkan perjalanan belajar Anda.</p></section></>}
